# 4. Написать программу, которая генерирует в указанных пользователем границах:
#
# случайное целое число;
# случайное вещественное число;
# случайный символ. Для каждого из трех случаев пользователь задает свои границы диапазона. Например, если надо получить
# случайный символ от 'a' до 'f', то вводятся эти символы. Программа должна вывести на экран любой символ алфавита от
# 'a' до 'f' включительно.

import random

'''
first, last = [int(i) for i in input('Введите границы диапазона целых значений через пробел: ').split()]
print('Случайное целое число:', format(random.randint(first, last)))


first, last = [float(i) for i in input('Введите границы диапазона вещественных значений через пробел: ').split()]
print('Случайное вещественное число:', format(random.uniform(first, last)))
'''
letters = list('abcdefghijklmnopqrstuvwxyz')
first, last = [str(i).lower() for i in input('Введите границы диапазона символьных значений через пробел: ').split()]
print('Случайный символ:', format(random.choice(letters[letters.index(first):letters.index(last)+1])))
